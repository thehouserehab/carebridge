# Carebridge · 아동발달 로컬 프로토타입

Carebridge는 치료사–보호자–센터가 아동의 목표, 기록, 가정 경험을 이어가는 가상 데이터 프로토타입입니다.

## 실행

Node.js 24에서 이 폴더를 열고 실행합니다. 런타임 의존성 설치가 필요하지 않습니다.

```powershell
npm run dev
```

http://127.0.0.1:4173 접속. 포트를 바꾸려면 `$env:PORT = '4174'` 설정 후 실행합니다. 서버는 로컬 컴퓨터에만 바인딩합니다.

```powershell
npm run check
npm test
npm run build
```

`dist`는 정적 빌드 출력입니다. 웹 루트로 제공해야 합니다. `scripts/server.mjs`는 개발 소스를 제공하며 보고서·테스트·설계 문서는 HTTP로 노출하지 않습니다.

## 체험 순서

1. 치료사 화면에서 하루의 공동 목표를 확인합니다.
2. 기록 작성에서 제목·공유 내용·내부 메모·선택적 평가·가정 활동을 저장합니다.
3. 공유 미리보기에서 보호자에게 보이는 내용만 확인하고 공유합니다.
4. 우측 상단 데모 화면을 보호자로 바꾸고 가정 활동의 경험을 남깁니다.
5. 치료사로 돌아와 피드백을 확인하고 다음 회기 반영 메모를 적습니다.
6. 다음 기록 작성에서 이전 피드백과 회기 메모를 펼쳐 참고합니다.
7. 센터 화면에서 새 가상 아동·일정을 등록하고 출결을 수정합니다.

치료사는 하루·나무 담당, 보호자는 하루에만 연결됩니다. 센터는 여울까지 기본 운영 정보를 봅니다. 새 아동은 보호자 계정에 자동 연결되지 않습니다.

## 구조

- `src/config.js`: 브랜드·데모 계정·저장 키
- `src/domain.js`: 관계·입력 검증·역할별 조회·상태 변경
- `src/repository.js`: 저장·읽기·손상 및 충돌 처리
- `src/seed.js`: 가상 데이터
- `src/ui.js`: 이스케이프·공통 UI
- `src/app.js`, `src/styles.css`: 역할별 화면·폼·반응형 디자인
- `docs/deployment.md`: 배포 설정, 검증 방법, 복구 방법
- `tests/domain.test.mjs`: 핵심 데이터 흐름·접근 경계·저장 실패 검증

## 저장과 한계

데이터는 브라우저 localStorage의 `child-development-demo-v2`에 저장됩니다. 새로고침과 서버 재실행 후 같은 브라우저·같은 주소에서 유지됩니다. 다른 기기·브라우저·포트에는 동기화되지 않고 브라우저 데이터 삭제 시 사라집니다. 이전 앱의 저장 키는 수정하지 않습니다.

역할 전환은 데모이며 실제 로그인이나 서버 권한 검증이 아닙니다. 브라우저 개발 도구에서는 로컬 데이터에 접근할 수 있습니다. 실제 아동·건강·연락처 등 개인정보를 입력하면 안 됩니다. 운영 전 서버 인증/권한, 데이터베이스, 동의·보관 정책, 감사, 백업이 필요합니다.

평가는 직접 입력한 값의 저장·비교만 지원합니다. 임상 평가 양식·자동 점수·진단·운동 처방·AI는 제공하지 않습니다. 가정 활동 예시는 화면 체험용이며 치료 지시가 아닙니다.

공유 시 별도 스냅샷을 만들어 내부 메모와 평가 원문을 제외합니다. 초안 수정은 기존 공유본에 영향을 주지 않으며 재공유해야 반영됩니다. 과거 피드백은 당시 공유본에 연결됩니다. 공유 철회는 현재 보호자 화면에서 숨기며 이미 열람한 내용은 회수할 수 없습니다.

사진·동영상은 세션 기록과 보호자 활동 피드백에 첨부할 수 있습니다. JPG/PNG/WebP/GIF 사진 10MB, MP4/WebM 영상 50MB 이하, 기록당 최대 6개·합계 100MB를 지원합니다. HEIC/MOV는 변환 후 첨부해야 하며 브라우저가 지원하지 않는 코덱은 미리보기가 제한됩니다.

파일은 IndexedDB `carebridge-media-v1`에, 메타데이터는 기존 localStorage에 저장합니다. 서버 업로드·외부 전송은 없습니다. 저장 전 선택한 파일은 임시 미리보기이며 취소하면 보관하지 않습니다. 치료사 첨부는 기본 비공유이고 파일별 선택 후 기록을 공유해야 보호자 화면에 나타납니다. 보호자 피드백의 첨부는 담당 치료사에게 함께 전달됩니다. 이는 데모 화면 분리이며 실제 접근 보안을 제공하지 않습니다.

파일 저장 실패 시 기록을 저장하지 않고, 기록 저장 실패 시 새 파일을 롤백합니다. 저장된 첨부를 기록에서 제거해도 과거 공유본 보존을 위해 원본은 즉시 삭제하지 않습니다. 브라우저 비정상 종료 시 참조 없는 파일이 남을 수 있고, 저장소 정리·백업·영구 삭제 정책은 후속 과제입니다. 브라우저 데이터 삭제·저장소 회수 시 파일이 사라질 수 있으므로 유일한 원본을 보관하지 마세요. 실제 아동 사진·영상을 사용하지 마세요.

`src/media-policy.js`는 형식·용량 검증, `src/media-store.js`는 파일 저장·실패 롤백, `src/media-ui.js`는 선택·미리보기·공유 범위를 담당합니다.

수납·정산·실시간 채팅·서버 파일 업로드·외부 알림·AI·카메라 측정은 후속 검토 대상입니다.

## 배포

Vercel은 `npm test && npm run build`를 실행하고 `dist/`만 공개합니다. GitHub `main`에 연결된 프로젝트에서 자동 배포하도록 구성합니다. 비밀키나 환경변수는 필요하지 않습니다. HTTPS 배포 주소와 로컬 주소는 서로 다른 저장소이므로 로컬에서 입력한 기록·첨부가 배포 사이트로 전송되거나 이전되지 않습니다.

내부 조사보고서·현장 자료·원본 첨부·로컬 테스트 산출물은 공개 저장소에 포함하지 않습니다. 이 저장소의 예시 데이터는 가상 데이터입니다. 기존 버전은 Git 이력에서 복구할 수 있습니다.
